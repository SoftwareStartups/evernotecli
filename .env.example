# Evernote OAuth 1.0a credentials (required for OAuth flow)
EVERNOTE_CONSUMER_KEY=
EVERNOTE_CONSUMER_SECRET=

# Direct token (skip OAuth if set)
EVERNOTE_TOKEN=

# Path to cached token file (default: ~/.evercli/token.json)
# EVERNOTE_TOKEN_PATH=~/.evercli/token.json

# Log level
LOG_LEVEL=info
